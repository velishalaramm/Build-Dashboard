#version: "3.8"

services:
  dashboard:
    build: .
    container_name: jenkins-dashboard
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: always
    volumes:
      - ./logs:/app/logs

